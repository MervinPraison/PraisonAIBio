# Offline SBML cache

Download curated demo models:

```bash
python scripts/download_demo_sbml.py
```

Expected files:

- `BIOMD0000000206.xml` — Teusink yeast glycolysis
- `BIOMD0000000012.xml` — MAPK cascade demo

Or use bundled slices in `demo_data/bundled_slices/` for offline search tests.
