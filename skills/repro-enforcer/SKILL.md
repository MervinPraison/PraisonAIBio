# Reproducibility Enforcer

Save models, capture metadata, and build reproducibility bundles.

## Tools

- `save_model(model_id, output_path)`
- `get_modelinfo(model_id)`
- `query_article(model_id)`

## Bundle layout

```
bundle/
  model.xml
  metadata.json
  README.md
```
