---
name: parameter-scan
description: Scan a parameter across a range
---

# Scan a parameter across a range

Use the `parameter_scan` tool after `import praisonai_bio`.

Example model: BIOMD0000000206
